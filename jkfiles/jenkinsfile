def utils
def cfg
pipeline {
    agent any
    stages{
        stage('loadCfg'){
                utils = load "./jkfiles/utils.groovy"
                cfg = utils.cfg
                echo "${cfg['name']}"
                def a = cfg['name']
                echo "${a} .... jenkinsfile"
            }
    }
}