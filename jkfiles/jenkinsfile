def utils
def cfg
node {
    stage('loadCfg'){
        utils = load "./jkfiles/utils.groovy"
        cfg = utils.cfg
        echo "${cfg['name']}"
        def a = cfg['name']
        echo "${a} .... jenkinsfile"
    }
}